---
id: T-048
title: 모바일 터치 이동 최적화
status: done
agent: game-engine-dev
skill: task-close
depends_on: [T-047]
source: user request
created: 2026-08-22
---

# T-048. 모바일 터치 이동 최적화

## 요청

모바일에서 실행하면 터치된 위치를 기준으로 이동 컨트롤러 처리를 해서 모바일 입력 처리를 최적화한다.

## 완료 조건 (DoD)

- [x] 터치/펜 입력은 첫 터치 위치를 가상 스틱 중심으로 사용한다.
- [x] 중심 주변 작은 흔들림은 이동으로 처리하지 않는다.
- [x] 터치 드래그는 일정 반경 이후 최대 속도 방향으로 제한된다.
- [x] 마우스 드래그와 키보드 이동은 기존 입력 흐름을 유지한다.
- [x] `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build` 가 통과한다.

## 결과

- 구현: `src/engine/input.ts`
- 테스트: `tests/src/input.test.ts`
- 문서: `docs/02-게임코어.md`, `docs/06-개선기록.md`
- 검증: `npm run typecheck`, `npm run lint`, `npm run test` (273개), `npm run build` 통과. 빌드 gzip 65.41KB / 예산 100KB
- 설계 차이: 별도 UI 스틱을 그리지 않고 입력 처리만 최적화했다. 화면을 가리지 않는 것이 전투 시야에 더 유리하다.
