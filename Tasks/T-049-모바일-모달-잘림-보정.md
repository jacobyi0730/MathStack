---
id: T-049
title: 모바일 퀴즈·스킬 선택 잘림 보정
status: done
agent: ui-builder
skill: task-close
depends_on: [T-048]
source: user request
created: 2026-08-22
---

# T-049. 모바일 퀴즈·스킬 선택 잘림 보정

## 요청

퀴즈나 스킬 선택 창이 모바일에서 잘려 보이는 경우가 있으므로, 세로 FHD 해상도를 기준으로 잘 보이도록 확인하고 보정한다.

## 완료 조건 (DoD)

- [x] 퀴즈 모달이 뷰포트보다 길어질 때 화면 밖으로 잘리지 않고 내부 스크롤된다.
- [x] 스킬 선택 창이 뷰포트보다 길어질 때 화면 밖으로 잘리지 않고 내부 스크롤된다.
- [x] 세로 FHD 모바일 기준으로 사용할 수 있도록 패널 최대 높이를 뷰포트 기반으로 제한한다.
- [x] 기존 2×2 퀴즈 보기와 터치 대상 크기는 유지한다.
- [x] `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build` 가 통과한다.

## 결과

- 구현: `src/ui/quiz-modal.ts`, `src/ui/skill-choice.ts`
- 테스트: `tests/src/quiz-modal.test.ts`, `tests/src/skill-choice.test.ts`
- 문서: `docs/05-세션-운영.md`, `docs/06-개선기록.md`
- 검증: `npm run typecheck`, `npm run lint`, `npm run test` (273개), `npm run build` 통과. 빌드 gzip 65.49KB / 예산 100KB
- 설계 차이: 별도 모바일 전용 레이아웃을 만들지 않고, 동일 UI가 세로 화면 안에서 스크롤되도록 제한했다.
- 환경 메모: 이 작업 환경의 PATH에서 Chrome/Edge 실행 파일을 찾지 못해 실제 브라우저 스크린샷은 남기지 못했다. 대신 DOM 스타일 계약 테스트로 `100dvh` 최대 높이와 내부 스크롤을 검증했다.
