---
id: T-051
title: 퀴즈 출제 랜덤성 보정
status: done
agent: game-engine-dev
skill: task-close
depends_on: [T-050]
source: user request
created: 2026-08-22
---

# T-051. 퀴즈 출제 랜덤성 보정

## 요청

매번 플레이할 때 퀴즈가 거의 비슷한 순서대로 나오는 느낌이 있으므로, 어떤 퀴즈가 나올지 예측하지 못하도록 랜덤성을 준다.

## 완료 조건 (DoD)

- [x] 새 플레이마다 퀴즈 세션 시드가 고정값으로 시작하지 않는다.
- [x] 브라우저 난수와 시간값을 섞어 런타임 시드를 만든다.
- [x] 같은 뱅크라도 세션 시드가 다르면 초반 문항 순서가 달라진다.
- [x] 기존 영역·난이도 분포 검증은 유지된다.
- [x] `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build` 가 통과한다.

## 결과

- 구현: `src/quiz/session.ts`, `src/main.ts`
- 테스트: `tests/src/quiz-session.test.ts`, `tests/src/quiz-selector.test.ts`
- 문서: `docs/04-수학시스템.md`, `docs/06-개선기록.md`
- 검증: `npm run typecheck`, `npm run lint`, `npm run test` (277개), `npm run build` 통과. 빌드 gzip 65.75KB / 예산 100KB
- 설계 차이: 뱅크 생성은 여전히 결정적이다. 예측 불가능성은 런타임 세션 시드에만 적용해 배포 산출물 재현성을 유지한다.
