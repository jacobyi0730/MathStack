# AGENTS.md — MathStack

> **저장소의 단일 진입점.** 이 문서는 세 가지만 담는다 — 프로젝트가 무엇인지(§1), 모르고 어기면 안 되는 규칙(§2), 나머지가 어디 있는지(§4).
>
> 상세는 전부 스펙·에이전트·스킬에 있다. **필요할 때 그것을 읽는다.**
> 규칙이 충돌하면 우선순위는 **AGENTS.md > 스펙 > 관행**. AGENTS.md가 틀렸다면 코드로 우회하지 말고 이 문서를 고친다.

---

## 1. 프로젝트

**MathStack** — 초등 3~6학년용 10분짜리 뱀서라이크. 움직이기만 하면 자동으로 싸우고, 레벨업할 때마다 **내 학년 교육과정에서 나온 진짜 수학 문제**를 풀어 다음 힘을 고른다.

| | |
| --- | --- |
| 세계관 | **주기율표 왕국** — 안정한 원소가 붕괴하는 원소에 맞선다 |
| 조작 | 이동만. 공격은 전부 자동 |
| 스택 | TypeScript 단일 언어 · Canvas 2D · Vite · Netlify 정적 배포 |
| 정본 | [기획서](docs/MathStack기획서.md) |

**설계 원칙 셋.** 판단이 갈릴 때 이 순서로 따진다.

1. **교육이 먼저다.** 문항이 학년 성취기준에 실제로 대응하지 않으면 이 프로젝트는 실패다.
2. **흐름을 끊지 않는다.** 문제는 전투 리듬의 일부다. 1문항 10~20초를 넘기면 게임이 학습지가 된다.
3. **오답은 처벌이 아니라 학습이다.** 체력을 깎지 않는다. 해설을 보여주고 나중에 다시 낸다.

---

## 2. 절대 규칙

**모르고 어길 수 있는 것들만 모았다.** 각 항목의 상세는 링크된 스펙에 있다.

| # | 규칙 | 어기면 | 상세 |
| --- | --- | --- | --- |
| 1 | **서버·API를 만들지 않는다** | Netlify 정적 배포가 깨진다 | [31-아키텍처](docs/30-기술/31-아키텍처.md) |
| 2 | **타입은 `shared/`에서만 온다** | 계약이 두 벌이 되어 조용히 어긋난다 | [31-아키텍처 §3](docs/30-기술/31-아키텍처.md) |
| 3 | **게임 루프에서 할당·`fetch` 금지** | 적 300체에서 프레임이 무너진다 | [34-성능예산](docs/30-기술/34-성능예산.md) |
| 4 | **문항 계산에 부동소수점 금지** | 오답이 오개념이 아니라 IEEE 754 때문에 틀린 값이 된다 | [36-코딩컨벤션 §3.1](docs/30-기술/36-코딩컨벤션.md) |
| 5 | **오답은 오개념에서 유도한다** | 이 게임을 만들 이유가 사라진다 | [33-데이터스키마 §3](docs/30-기술/33-데이터스키마.md) |
| 6 | **수치의 정본은 문서다** | 코드가 정본이 되면 기획이 죽는다 | [43-기여규칙 §1.3](docs/40-운영/43-기여규칙.md) |
| 7 | **아이템·적 이름은 원소 × 원자번호** | 명명이 무의미해지고 레퍼런스와 겹친다 | [기획서 §6.1](docs/MathStack기획서.md#61-명명-규칙--원소--수학-) |
| 8 | **`public/data/`·`dist/`는 생성물** | 손으로 고치면 다음 빌드에 날아간다 | [31-아키텍처 §8](docs/30-기술/31-아키텍처.md) |
| 9 | **`mds/`는 읽기 전용** | 사용자의 원본 요청 기록이다 | — |

> 1·3·4는 **협상 대상이 아니다.** 가독성·편의를 이유로 완화하지 않는다. 정말 필요하다고 판단되면 코드를 쓰기 전에 사용자에게 확인한다.

---

## 3. 실행

```powershell
npm install
npm run build:bank && npm run validate:bank    # 문항을 고쳤을 때마다
npm run dev                                     # http://localhost:5173
```

전체 명령과 Netlify 재현 순서는 [31-아키텍처 §7](docs/30-기술/31-아키텍처.md).

---

## 4. 문서 인덱스

> 상태: ✅ 작성 완료 · 🚧 작성 중 · 📝 예정
> **표에 없는 문서는 없는 문서다.** 새 문서를 만들면 `new-doc` 스킬로 여기 등록한다.

### 4.1 기획 (`docs/`)

| # | 문서 | 다루는 것 | 상태 |
| --- | --- | --- | --- |
| 00 | [MathStack기획서.md](docs/MathStack기획서.md) | **전체 정본.** 컨셉·세계관·타임라인·무기/적·수학 문제 시스템·커리큘럼 매핑·로드맵 | ✅ |

### 4.2 게임 디자인 (`docs/10-게임디자인/`)

| # | 문서 | 다루는 것 | 상태 |
| --- | --- | --- | --- |
| 11 | [11-코어루프.md](docs/10-게임디자인/11-코어루프.md) | 세션 구조, 웨이브 타임라인, 승패 조건 | 📝 |
| 12 | [12-에이전트-적명세.md](docs/10-게임디자인/12-에이전트-적명세.md) | 적 카탈로그·AI 행동·스폰 규칙 | 📝 |
| 13 | [13-스킬-무기명세.md](docs/10-게임디자인/13-스킬-무기명세.md) | 무기·보조·각성 카탈로그, 밸런스 수치 | 📝 |
| 14 | [14-캐릭터-아트가이드.md](docs/10-게임디자인/14-캐릭터-아트가이드.md) | 원+눈 규격, 팔레트, 이펙트 | 📝 |
| 15 | [15-성장곡선.md](docs/10-게임디자인/15-성장곡선.md) | 경험치 곡선, 레벨업 간격, 난이도 상승률 | 📝 |

### 4.3 수학 커리큘럼 (`docs/20-수학커리큘럼/`)

| # | 문서 | 다루는 것 | 상태 |
| --- | --- | --- | --- |
| 21 | [21-학년별성취기준.md](docs/20-수학커리큘럼/21-학년별성취기준.md) | 3~6학년 영역·단원·성취기준 코드 매핑 | 📝 |
| 22 | [22-문제유형카탈로그.md](docs/20-수학커리큘럼/22-문제유형카탈로그.md) | 유형별 템플릿과 파라미터 생성 규칙 | 📝 |
| 23 | [23-출제-난이도규칙.md](docs/20-수학커리큘럼/23-출제-난이도규칙.md) | 레벨↔난이도 매핑, 오답 재출제, 오답지 생성 | 📝 |

### 4.4 기술 (`docs/30-기술/`)

| # | 문서 | 다루는 것 | 상태 |
| --- | --- | --- | --- |
| 31 | [31-아키텍처.md](docs/30-기술/31-아키텍처.md) | **빌드타임/런타임 경계, `shared/` 계약, 디렉터리, 실행** | ✅ |
| 32 | [32-콘텐츠파이프라인.md](docs/30-기술/32-콘텐츠파이프라인.md) | 템플릿 DSL, 전개·검증 규칙, 산출물 계약 | 📝 |
| 33 | [33-데이터스키마.md](docs/30-기술/33-데이터스키마.md) | **문항·밸런스·localStorage 스키마, 검증 항목** | ✅ |
| 34 | [34-성능예산.md](docs/30-기술/34-성능예산.md) | **프레임 예산, 게임 루프 규칙, 측정 기록** | ✅ |
| 35 | [35-배포.md](docs/30-기술/35-배포.md) | `netlify.toml`, 캐시 헤더, 롤백 | 📝 |
| 36 | [36-코딩컨벤션.md](docs/30-기술/36-코딩컨벤션.md) | **TypeScript·빌드타임·런타임 규칙, 테스트** | ✅ |

### 4.5 운영 (`docs/40-운영/`)

| # | 문서 | 다루는 것 | 상태 |
| --- | --- | --- | --- |
| 41 | [41-개발환경셋업.md](docs/40-운영/41-개발환경셋업.md) | Windows 기준 셋업, 트러블슈팅 | 📝 |
| 42 | [42-테스트전략.md](docs/40-운영/42-테스트전략.md) | 테스트 범위, 밸런스 회귀 | 📝 |
| 43 | [43-기여규칙.md](docs/40-운영/43-기여규칙.md) | **문서 작성 규칙, Git, 완료 기준** | ✅ |

### 4.6 에이전트 (`.claude/agents/`) — 누가

| 에이전트 | 담당 영역 |
| --- | --- |
| [quiz-author](.claude/agents/quiz-author.md) | `content/` — 문항·템플릿 저작, 오개념 오답 설계 |
| [pipeline-engineer](.claude/agents/pipeline-engineer.md) | `tools/`, `shared/` — 전개·검증·스키마 |
| [game-engine-dev](.claude/agents/game-engine-dev.md) | `src/engine·systems·entities` — 루프·충돌·렌더·AI |
| [ui-builder](.claude/agents/ui-builder.md) | `src/ui/` — HUD·퀴즈 모달·접근성 |
| [balance-tuner](.claude/agents/balance-tuner.md) | 무기·적·성장곡선 수치, 문서↔데이터 동기화 |
| [deploy-ops](.claude/agents/deploy-ops.md) | `netlify.toml`·빌드·번들 예산 |
| [doc-keeper](.claude/agents/doc-keeper.md) | `docs/`·인덱스·작업 보드 정합성 |

### 4.7 스킬 (`.claude/skills/`) — 어떻게

| 스킬 | 하는 일 |
| --- | --- |
| [add-enemy](.claude/skills/add-enemy/SKILL.md) | 적 1종을 문서→데이터→코드 순서로 추가 |
| [add-weapon](.claude/skills/add-weapon/SKILL.md) | 무기 1종을 각성 짝꿍까지 포함해 추가 |
| [add-question-template](.claude/skills/add-question-template/SKILL.md) | 문항 템플릿을 오개념 오답과 함께 추가 |
| [verify-curriculum](.claude/skills/verify-curriculum/SKILL.md) | 성취기준 코드·학년·학기·적합도 검증 |
| [build-bank](.claude/skills/build-bank/SKILL.md) | 뱅크 굽기·검증, Netlify 빌드 재현 |
| [perf-check](.claude/skills/perf-check/SKILL.md) | 적 300체 시나리오 프레임 측정 |
| [new-doc](.claude/skills/new-doc/SKILL.md) | 문서 생성 + 인덱스 등록 |
| [task-close](.claude/skills/task-close/SKILL.md) | DoD 확인·보드 동기화·후속 해제 |

### 4.8 작업 — 무엇을 언제

| 문서 | 내용 |
| --- | --- |
| [TodoList.md](TodoList.md) | **완료 상태의 유일한 정본.** 체크박스·잠금·병렬 배정 이력 |
| [Tasks/README.md](Tasks/README.md) | **작업 보드.** 28개 태스크의 구조 — Phase·담당·스킬·선행·병렬 트랙 |

### 4.9 원본 (`mds/`) — 읽기 전용

| 문서 | 내용 |
| --- | --- |
| [1. 기획서 작성 요청](mds/1.%20기획서%20작성%20요청.md) | 게임 컨셉·커리큘럼·레퍼런스 |
| [2. 개발진행 요청](mds/2.%20개발진행%20요청.md) | 오케스트레이터-에이전트-스킬 계층, Tasks 구성 |
| [3. 원소이름](mds/3.%20원소이름.md) | 화학 원소 목록 |
| [4. AGENTS의 내용 정리 요청](mds/4.%20AGENTS의%20내용%20정리%20요청.md) | AGENTS.md 슬림화, `.gitignore` |

---

## 5. 오케스트레이션

### 5.1 4계층

각 계층이 **다른 질문에 답한다.** 내용을 어디에 둘지 헷갈리면 질문 형태로 판단한다.

| 계층 | 답하는 질문 | 위치 | 로드 시점 |
| --- | --- | --- | --- |
| **헌법 + 인덱스** | 어기면 안 되는 것은? 나머지는 어디? | `AGENTS.md` | **항상** |
| **스펙** | 무엇이 참인가? (수치·계약·구조) | `docs/` | 필요할 때 |
| **에이전트** | 이 영역에서 무엇이 옳은가? (판단·거부 기준) | `.claude/agents/` | 위임될 때 |
| **스킬** | 이 작업을 빠뜨림 없이 하려면? (절차·DoD) | `.claude/skills/` | 호출될 때 |

**AGENTS.md는 모든 맥락에 로드된다.** 여기 있는 한 줄은 모든 작업이 비용을 치른다. 그래서 "모르고 어길 수 있는 것"만 남기고 나머지는 전부 내보냈다.

**중복 금지 원칙** — 수치와 표는 **스펙에만** 둔다. 에이전트는 원칙을 말하고 스펙을 링크한다. 같은 숫자가 두 곳에 있으면 반드시 어긋난다.

### 5.2 작업 절차

1. **[TodoList.md](TodoList.md)에서 `[ ]` 태스크를 고른다.** 여기가 완료 상태의 유일한 정본이다. `[~]`는 남이 잡고 있는 것, `[-]`는 선행 미완.
2. 구조(선행·담당·스킬)는 [Tasks/README.md](Tasks/README.md), 상세는 태스크 파일에서 읽는다.
3. TodoList 의 해당 줄을 **`[~]` 로 잠근다.** 그다음 실행자에게 위임한다.
4. 실행자는 명시된 **스킬 절차**를 따르고 필요한 **스펙**을 읽는다.
5. **`task-close` 스킬**로 마감하고 TodoList 를 `[x]` 로 바꾼다.

### 5.3 병렬 실행

의존 그래프상 **세 트랙이 거의 독립적으로 흐른다**(Tasks/README.md 「병렬 트랙」). 겹치지 않는 트랙은 동시에 돌린다.

**동시 배정 전 반드시 확인한다.**

| 확인 | 왜 |
| --- | --- |
| 두 태스크의 **산출 파일 집합이 겹치지 않는가** | 같은 파일을 동시에 쓰면 나중 쓰기가 앞선 것을 지운다 |
| 둘 다 `shared/` 를 고치지 않는가 | 계약 파일은 트랙 경계를 넘는다. 한 번에 하나만 |
| `TodoList.md`·`Tasks/README.md` 를 실행자가 건드리지 않는가 | 상태 갱신은 **오케스트레이터 전담**. 여러 실행자가 같은 줄을 고치면 그 자체가 충돌 |

**실행자에게 넘길 때는 건드려도 되는 경로를 명시적으로 못 박는다.** "T-004 해줘"가 아니라 "T-004 를 하되 `src/engine/renderer.ts`·`camera.ts`·`src/entities/sprite.ts`·`src/data/palette.ts` 만 건드려라".

### 5.4 실행자 선택 — Claude 에이전트 vs codex

작업 성격에 따라 고른다. 둘 다 같은 저장소에서 돌지만 강점이 다르다.

| | **Claude 서브에이전트** | **codex CLI** |
| --- | --- | --- |
| 적합 | 설계 판단, 성능 트레이드오프, 미묘한 규칙 해석 | **명세가 촘촘한 대량·기계적 작업** |
| 예 | 게임 루프 구조, 풀 자료구조 선택, 밸런스 조정 | 커리큘럼 표 100여 항목 전사, 반복 패턴 구현, 대량 문항 템플릿 |
| 호출 | `Agent` 도구 (`.claude/agents/` 정의) | `codex exec` (아래) |

```powershell
codex exec -C "c:/GithubProjects/MathStack" `
  -s workspace-write `
  -o .codex-out/T-016.md `
  "<태스크 지시문>"
```

- `-s workspace-write` — 파일 쓰기 허용. `read-only` 는 조사 전용
- `-o <FILE>` — 마지막 메시지를 파일로. **결과 확인은 이 파일로 한다**
- `-C` — 작업 루트 고정

**codex 사용 시 주의**

- **stdout 의 한글이 깨져 보인다.** Windows 콘솔 코드페이지 문제이고, **파일은 UTF-8 로 정확히 쓴다**(실측 확인). 터미널 출력만 믿지 말고 파일을 직접 검증한다.
- codex 에게도 **AGENTS.md 와 관련 스펙을 읽으라고 지시문에 명시한다.** 이 저장소의 규칙은 자명하지 않다.
- 끝난 뒤 **오케스트레이터가 게이트를 직접 돌린다** — `typecheck`·`lint`·`test`·`build`. 실행자의 "완료했다"를 그대로 믿지 않는다.

### 5.5 라우팅

| 건드리는 것 | 에이전트 |
| --- | --- |
| `content/` 문항·템플릿 | **quiz-author** |
| `tools/`, `shared/` | **pipeline-engineer** |
| `src/engine·systems·entities` | **game-engine-dev** |
| `src/ui/`, `index.html`, CSS | **ui-builder** |
| 무기·적·곡선 **수치** | **balance-tuner** |
| `netlify.toml`, 빌드 설정 | **deploy-ops** |
| `docs/`, 인덱스, 보드 | **doc-keeper** |

경계에 걸치는 작업은 태스크 파일의 `agent` 필드를 따른다. "새 적 추가"는 세 곳을 건드리지만 `add-enemy` 스킬이 순서를 정해주므로 game-engine-dev 하나가 처리한다.

### 5.6 위임하지 않고 사용자에게 묻는 경우

- 기획서의 **설계 결정을 뒤집는** 변경 (오답 체력 페널티, 세션 길이 등)
- **서버 도입** 여부 (§2 규칙 1을 깨는 결정)
- 태스크 **범위 변경**이나 새 태스크 추가
- 성능 예산·접근성 기준 **완화**

에이전트는 이런 판단을 스스로 하지 않는다. 막히면 멈추고 보고한다.
